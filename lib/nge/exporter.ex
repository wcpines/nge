# lib/nge/exporter.ex

defmodule Nge.Exporter do
  # alias Nge.CSVParser
  # alias Nge.Api

  # require Logger
  # use GenServer

  # def run(auth_code, options \\ []) do
  #   GenServer.call(__MODULE__, {:run, auth_code})
  # end

  # def handle_call({:run, auth_code, options}, _from, _empty_map) do
  # end

  # def init(init_arg) do
  #   {:ok, init_arg}
  # end

  # def start_link(_opts) do
  #   {:ok, _pid} = GenServer.start_link(__MODULE__, [], name: __MODULE__)
  # end
end
